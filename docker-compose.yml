version: "3"
# this is a yaml file that contains all the configurations for the services in the project

services:
  app:
    build:
      context: .
    ports:
      - "8000:8001"
    volumes:
      - "./app:/app"
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"